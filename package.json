{
  "name": "clubhub-root",
  "version": "1.0.0",
  "scripts": {
    "build": "if [ \"$RENDER\" != \"true\" ]; then npm --prefix frontend install && npm --prefix frontend run build && rm -rf dist && cp -r frontend/dist ./dist; else echo 'Skipping frontend build on Render...'; fi",
    "start": "cd backend && npm install && node server.js"
  }
}
